/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 */
'use strict';

var React = require('react-native');
var {
  AppRegistry,
  NativeModules,
  StyleSheet,
  Text,
  View,
} = React;

NativeModules.RNGeocoder.geocodeAddress(
  'Paris France',
  (err) => {
    console.log('ERROR GEOCODING', err);
  },
  (res) => {
    console.log('RESULTS', res);
  });

NativeModules.RNGeocoder.reverseGeocodeLocation(
  { latitude: 48.856614, longitude: 2.3522219},
  (err) => {
    console.log('ERROR GEOCODING', err);
  },
  (res) => {
    console.log('RESULTS', res);
  });

var GeocodingAppSample = React.createClass({
  render: function() {
    return (
      <View style={styles.container}>
        <Text style={styles.welcome}>
          Welcome to React Native!
        </Text>
        <Text style={styles.instructions}>
          To get started, edit index.android.js
        </Text>
        <Text style={styles.instructions}>
          Shake or press menu button for dev menu
        </Text>
      </View>
    );
  }
});

var styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#F5FCFF',
  },
  welcome: {
    fontSize: 20,
    textAlign: 'center',
    margin: 10,
  },
  instructions: {
    textAlign: 'center',
    color: '#333333',
    marginBottom: 5,
  },
});

AppRegistry.registerComponent('GeocodingAppSample', () => GeocodingAppSample);
